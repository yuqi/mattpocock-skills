---
"mattpocock-skills": major
---

Keep the existing Issue Tracker contract while making local markdown under `.scratch/` its only shipped implementation. Setup no longer offers provider selection, the GitHub and GitLab tracker templates are removed, wayfinder follows the configured single-writer local map operations, `to-spec` loads referenced resolved decisions from a local map, and triage uses local ticket fields and sections.
