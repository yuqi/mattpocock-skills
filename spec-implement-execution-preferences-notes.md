# spec-implement 执行偏好讨论记录

日期：2026-09-05

状态：讨论记录，不是技能指令。用户随后授权调整启动配置；已落地的子集见下节，其余效率建议仍待实施。

## 启动配置落地进度

2026-09-05：按后续授权，在 `spec-implement` 中加入 `.scratch/<feature>/spec-implement.json` 的初始化、保存与恢复规则。新配置默认均衡，保存语义偏好、明确用户限制，以及各任务最近的请求与观测设置。主代理结合任务、上下文和运行环境自主选择，不固定模型或推理级别；委派 owner 继续传递同一偏好和约束。

恢复时验证配置格式、版本和 spec 身份，当前明确指令覆盖对应已存设置，未修改的限制继续保留。历史选择仅供参考，无法满足显式限制时不能静默降级。配置由 scheduler 在集成事务之间保存，保留到运行结束，不进入实现、修复或评审提交，也不能代替 Git 和持久化验收核验。

同步了路由说明和 beta 目录描述。本文后续“尚未实现”“暂不修改”等表述保留为讨论时点记录，以本节和实际技能文件为当前状态。容量恢复、截断补读、评审覆盖核验等其它调整仍未实施；本次也不修改安装或全局模型配置。

验证：`spec-implement` 通过技能校验，JSON 示例可解析，本地引用可达，`git diff --check` 通过。Terra medium 独立模拟了新启动、不可用模型限制、临时偏好与正在集成的 worker 返回、配置损坏或身份错配、全票已验收后的恢复五类场景；据此补清临时配置和 closeout 停止边界，复核未发现这三类受影响场景中的实质矛盾。未执行真实 spec 的端到端运行。`ask-matt` 校验器拒绝其原有 `disable-model-invocation` 字段，仓库要求的兼容元数据保持不变。

## 当前建议的调整方向

本节汇总前面的设计讨论和原始日志核验，作为拟实施方向；后续各节保留证据与结论演进。

目标是提高可靠完成整份 spec 的效率。硬约束继续是任务范围、实现隔离、集成单写者、独立评审、固定需求与提交边界、持久化验收和最终全量验证。排序标准沿用：验收正确性与独立性 > 流程可继续性和可恢复性 > 用户选择下的端到端耗时与资源消耗 > 规则维护负担。具体模型收益、实际费用和修改后的改善幅度仍需运行证据，不能由型号或推理级别推定。

### 1. 用户选择意图，主代理决定具体配置

- **质量优先：** 优先提高正确性、完整性和判断可信度，接受有预期收益的额外推理、上下文理解和执行成本。
- **均衡：** 综合质量收益、完成耗时和资源消耗，把更多投入放在困难、影响大的工作上。建议作为未指定时的默认值。
- **速度优先：** 优化通过全部验收的总时间，将上下文获取、交接、集成、评审和返工计入，不能只优化单次响应时间。

技能不固定型号、推理级别或角色组合。主代理在既有职责与独立性约束内，把任务边界、上下文交接、模型和推理投入作为一次联合决策：考虑任务难度与错误代价、所需上下文与已有理解的关系、重新理解的成本、当前容量与关键依赖、运行时可用选项和相关实际效果。模型名、最高档位或低单价本身都不是决定依据。

明确允许主代理为每次子代理创建选择模型与推理设置，无须逐次向用户确认；用户明确指定的模型限制、资源上限和运行环境能力优先。子代理再委派或调用评审技能时，传递相同偏好及必要约束。设置须通过运行环境实际支持的方式生效；不支持时如实报告并使用合法继承或工作流后备路径，不虚构切换能力。

失败后先判断是上下文缺失、实现缺陷、能力不足还是运行环境故障，再调整任务、交接或配置；不把任何一次 Block 自动归因为模型过弱。记录实际设置和一句任务及上下文依据，设置选择随证据调整，用户偏好随用户明确修改而改变。

### 2. 控制整个代理树的开销

根会话保留任务图、claims、权威路径、验收状态和必要决策，读取足够证据确认持久化验收，避免同时重新做完整子代理分析。原始长日志和实现探索留在负责该任务的上下文或可检索文件中，交接传递紧凑结果和证据指针。

独立工作可委派，但将根会话调度开销、上下文重建、嵌套代理及合并成本一并考虑。可确定的批量提取和统计使用本地工具；模型读取会影响判断的结果。长历史的恢复需要显式检查证据和状态，短提示并不等于短上下文。

本次分析实测也支持把根会话纳入优化：从首次评估跟踪报告到上一轮审计结论，合计 6,884,929 token，其中根 Astra 为 5,058,742，约 73%；大部分输入为缓存命中。它不是固定模型对照实验，但说明增加廉价子代理并不能自动消除昂贵根会话的重复输入。

### 3. 把关键执行规则落在阶段边界

- **派发：** 明确核对当前阶段可用容量、任务和上下文输入，预留 merger 及独立 reviewer 所需容量。资源不足时区分等待可释放容量与安全移交现有 owning session，不复用带旧票据评审历史的代理来规避审查隔离。
- **读取：** 按任务大小选择直接读取或可控批次；大 diff 保留固定边界和排除规则，截断后补未读部分。强制完整读取照常执行，同一版本已完整读取的材料仅在有证据缺口时重读。
- **评审：** 提供独立、固定输入，覆盖全部 AC 和当前变更适用的硬规范。owner 核对覆盖没有缺失或重复、状态和 finding 分类完整，再按已有 gate 形成 verdict。复审可关注修复风险，但关注点不替代全票验收。
- **等待：** 有独立工作就继续，没有则依赖事件等待；超时和进度更新遵守有效上层指令，仅在需要恢复核验或处理异常时查状态列表。
- **恢复：** 正常运行的安全边界保留紧凑恢复信息；中断后先核对 Git、需求、持久化验收和旧代理，再续用或移交。无 verdict 的运行失败单独统计，不算实现修复轮次。

这些约定三档共用，不能通过选择速度优先跳过，也不因为质量优先而扩大范围、重复全量测试或无界增加代理。

### 4. 修改范围与验证方式

优先修改 `spec-implement` 的选择与调度规则，以及 `references/ticket-execution.md` 的读取、交接、等待和恢复约定。reviewer 输入隔离、返回完整性核验及 Standards 覆盖属于 `ticket-review`，累计审查的相同约定在 `spec-review` 对齐，不把这些要求只藏在根调度器里。`code-review` 的通用审查流程本次不另行重构。落地时按仓库规则同步涉及的路由、目录描述和已推广技能 docs，避免创建第二套验收流程。

通过代表性任务验证：资源不足可正确后备、大 diff 截断能完整补读、缺项结果不能验收、旧规范违规能在首轮形成有依据的 finding、中断恢复不重复完成工作、不同偏好仍守同一验收门槛。比较端到端耗时、根与子代理分项用量、重复读取和空等待、实际缺陷及误报和修复轮次；不设全局工具调用或 token 次数硬指标。

主要代价是自主路由的可重复性较弱，并增加少量状态和覆盖核验。对小票据采用紧凑形式。若确有严格复现或固定资源要求，再引入用户显式配置；若额外规则只增加开销且没有改善正确性或完成效率，就收窄规则。本次用户接受千万级分析 token 的表态不被转换为该技能所有运行的固定预算。

相对早期固定组合表，变化来自用户明确改为语义偏好和任务及上下文联合判断；相对初步运行报告，原始证据确认了规则已加载、旧问题迟发现、重复截断，以及本次分析中根会话的用量占比，因此把重点落到实际执行核验和整个代理树的成本。这是现阶段推荐方向，不是已经验证的性能收益。

## 已明确的设计方向

- 提供“质量优先”“均衡”“速度优先”三种偏好，定义各自的意义，不固定模型名称或推理级别，也不按角色维护固定组合表。
- 主代理自主决定具体配置，但偏好只是决策输入之一。必须结合实际任务、当前上下文与任务的关系、交接成本和运行环境作判断。
- 模型选择、推理投入、任务拆分和上下文传递应联合考虑，不能只根据偏好名称或任务角色直接映射配置。
- 此前讨论中的 Astra、Sol、Terra、Luna 和各推理档位表只是候选建议，不是已接受的实现要求。用户曾表达对 Terra、Luna 信心不足，这不构成它们实际能力不足的证据，也不自动成为永久禁用名单。

## 三种偏好的工作定义

以下是待落地时完善的语义草案：

| 偏好 | 优化目标 | 需要避免的误解 |
| --- | --- | --- |
| 质量优先 | 优先提高正确性、完整性与判断可信度，愿意为预期有效的深入推理和上下文理解投入更多时间与资源 | 不等于所有任务都选最高配置，也不意味着增加无关范围或无收益的重复检查 |
| 均衡 | 综合质量收益、端到端耗时和资源消耗，把投入集中在困难且影响大的决策上 | 不等于机械地选择中间型号或中间推理档位 |
| 速度优先 | 缩短从开始执行到通过验证的总时间，包含上下文获取、交接、集成、评审和返工 | 不等于选择单次响应最快的模型、尽量多开代理或减少必要验收 |

“未指定时使用均衡”是当前建议的默认值，尚未实现。

## 主代理应综合考虑的信息

1. 任务本身：难度、歧义、影响范围、错误代价，以及实现、集成、修复或独立评审的具体职责。
2. 上下文关系：任务依赖哪些需求、代码、决策和失败证据；父代理已有多少相关理解；新代理需要重建多少信息；摘要或路径指针能否保留关键关系。
3. 上下文质量：已有历史是否仍相关、是否过时、是否包含大量无关过程，以及复用历史是否影响独立评审。
4. 执行成本：可用代理槽位、依赖关系、集成瓶颈、交接和重新理解的耗时、预期返工成本。
5. 可用能力与约束：当前环境实际支持的模型、推理配置和历史共享方式，以及用户明确提出的限制。
6. 新证据：任务展开后的真实复杂度、测试或评审发现、上下文缺口和配置效果。根据这些信息调整后续决策，不机械重复初始选择。

## 建议保留的共同边界

- 三种偏好采用相同的任务范围、验收条件、独立评审、持久化验收与最终验证要求。
- 运行环境不支持配置或历史共享时，如实说明限制，不声称已经切换模型或配置。
- 用紧凑记录说明实际选择及其任务和上下文依据，便于回溯，不要求每次选择都打断用户确认。
- 自主选择不意味着跨越既有实现隔离、集成单写者或独立评审边界。

## 相对早期建议的变化

原建议是固定模型与推理组合，随后收敛到以 Sol 为基础的配置表。用户明确修改了设计要求：只定义偏好意义，把配置选择交给主代理，并补充任务与上下文关系作为必要输入。因此不再保留固定组合表作为推荐实现。

该方向的优势是能适应任务差异与环境变化；主要代价是不同运行的选择可能不同，更依赖主代理判断。若后续需要严格复现、固定预算，或实际观察到持续误选，再讨论显式配置或更窄的选择边界。

## 待后续落地时确定

- 偏好的输入表达、恢复时的保留方式，以及默认值。
- 传递给 merger 和独立评审子树的最小决策上下文。
- 选择记录的粒度，以及验证选择效果的代表性任务。

当前不修改 SKILL、路由器、元数据或安装配置。

## 实际运行记录的补充评估

日期：2026-09-05。以下仍是待讨论的调整建议，不是已实施的技能规则。

### 证据与决策框

初次评估时已完整阅读 [运行跟踪记录](./spec-implement-review-efficiency-01a06f81.md)，并对照当前 `spec-implement`、`ticket-execution`、`ticket-review`、`spec-review` 和 `code-review`。当时原始会话日志尚未取得，无法独立核对调用统计、具体实现缺陷，或确认执行时技能版本与当前版本完全一致。跟踪记录截至 07:18:56 UTC，尚未完成整个 spec；它不是端到端成功或成本对照实验。随后已取得原始日志，更新结论见文末“原始日志核验”；该节优先于这里的初步判断。

- 目标：保留可靠验收，降低等待、上下文传输和重复协调的成本，减少资源不足或中断导致的流程停滞。
- 硬约束：实现隔离、集成单写者、独立评审、需求和提交边界、持久化验收与最终验证不变；本轮只记录，不改 SKILL。
- 标准优先级：验收正确性与独立性 > 流程可继续性和可恢复性 > 端到端耗时与实际资源消耗 > 新规则的维护负担。
- 未知项：实际占槽代理清单与释放语义、历史技能版本、工具调用与模型响应轮次的准确对应、各类检查的必要性、缓存失效及 429 的服务端原因。

### 建议优先调整的内容

| 优先级 | 建议 | 证据与当前规则 | 主要落点 |
| --- | --- | --- | --- |
| P0 | 显式管理集成和 reviewer 所需容量，处理创建失败与安全移交 | 跟踪记录第 235–253 行出现 reviewer 创建失败、复用旧 reviewer。`spec-implement` 第 32–36 行已有释放槽位、预留 reviewer 和根会话接管原则，但缺少可操作的失败分支 | `spec-implement` 调度规则 |
| P0 | reviewer 使用独立审查上下文，校验返回覆盖后再形成 verdict | 跟踪记录第 274 行确认继承 merger 历史。`ticket-review` 第 22 行移除旧 Review，第 49–53 行要求新建 reviewer，但没有明确历史隔离或 owning session 的覆盖完整性检查；`spec-review` 第 69 行已经有缺失和重复覆盖校验 | `ticket-review`；共用原则适用于 `spec-review` |
| P1 | 按输入大小规划读取批次，跟踪已返回范围，覆盖 implementer、merger 和 reviewer | 跟踪记录第 275 行记录同一大 diff 以更大预算重读，第 331 行记录 Ticket 05 共 128 个工具轮次、累计输入 31,598,285 | `ticket-execution` 与各评审技能的读取流程 |
| P1 | 等待以事件和必要检查点驱动，只传递紧凑状态 | 跟踪记录第 24 行有 264 次等待、220 次超时；第 187–190、305 行显示长等待减少空唤醒，但长根上下文仍有持续成本 | `spec-implement` 与 merger 等待规则 |
| P1 | 在正常运行的安全边界保留恢复信息，区分运行失败与验收 Block | 跟踪记录第 237–247 行有无 verdict 的 429 中断与恢复时低缓存命中；`ticket-execution` 第 56 行要求失败时保留工作，但缺少恢复步骤 | `spec-implement` 与 `ticket-execution` |
| P2 | 给适用硬规范建立紧凑证据覆盖，与 AC 覆盖分别核对 | 跟踪记录第 80–82 行称错误码所有权违规直到第三轮才被发现；当前 reviewer brief 要求报告规范问题，但没有显式逐项覆盖核对 | `ticket-review`、`spec-review` 的 Standards brief |

P0 的排序依据是它们影响审查能否启动及能否可靠验收；不代表记录已经出现错误验收。P1 有较直接的运行成本证据。P2 有合理价值，但不能仅凭这份记录确定迟发现是首轮漏检还是中间修复引入，需原始 diff 验证。

### 落地时需要把握的边界

**容量管理。** 派发前按当前阶段计算所需容量，包括嵌套 reviewer，并按运行环境核实完成代理是否仍占槽。不要把“暂停”“完成”“释放”当作同义词，也不要假设工具提供关闭能力。集成前可采用现有根会话接管分支；已经由 merger 持有写锁时，先确认其安全停止，再移交阶段、固定点、Git 操作状态和未完成工作。容量会释放时可以等待；容量不会释放时，单纯等待不能解决问题。累计评审还需考虑三个独立轴的容量，不能默默合并为一个 reviewer 来省槽。

**独立审查与返回契约。** 给 reviewer 自包含、固定版本的需求与源码证据，隔离实现过程、旧 verdict 和其它票据的评审历史；使用运行环境支持的历史隔离方式，不把具体参数写成跨平台保证。owning session 在写入前校验每条 AC 恰好有一个合法状态和证据、所有 finding 有分类，信息不完整时不形成 verdict。独立性是调度自主权的边界，不随执行偏好改变。

**读取与验证。** 先建立与任务匹配的输入清单，必要时检查变更文件数、大小与 diff 统计，再选整读或按文件、行段读取。全部读取仍保留评审原有固定提交、路径排除和强制规范读取规则。并行合并彼此独立且输出可控的读取；被截断时补未返回部分，源码变化或证据确有缺口时允许重读。强制完整读取不能被摘要替代。必要测试继续执行，只批量协调互不干扰的检查，避免把 TDD 的依赖步骤或共用数据库测试盲目并行。

**等待与输出。** 有独立工作就继续，没有就等待消息。等待时限受当前有效指令、用户更新要求和下一个必要检查点共同约束，不写死分钟数。无状态变化时继续等待；`list_agents` 用于恢复、状态冲突或缺失完成信息。根会话只保留调度和验收需要的紧凑状态，原始日志留在需要它们的执行上下文或可检索文件中。仍须完整保留实际 Blocking 和 Non-blocking 发现，不用“精简输出”压掉评审结果。

**恢复。** 不能等到模型已被服务错误中断后才依赖它写恢复信息。正常运行时在安全阶段边界保留 spec/ticket 指针、agent 与 worktree、阶段、内部固定点、claim 与集成所有者、未提交工作、需求快照和验证证据位置。恢复前核对当前 Git、需求、持久化验收和仍活跃的旧代理，再决定续用原代理还是安全移交；摘要不是验收证据。短 `followup_task` 可以避免重复粘贴，但不能单凭短提示保证清除原会话长历史。没有有效 verdict 的运行失败单独统计，不算实现修复轮次；新 review 仍需满足既有固定输入和独立性要求。

**Standards 覆盖。** 只记录对当前变更适用的硬规则、证据和结果，避免把整本规范或每个启发式 smell 都变成永久大型清单。对“某问题为何晚一轮发现”区分旧问题漏检与修复后新回归，两者需要不同的改善措施。

### 不建议直接照搬的调整

- **统一 12–16 个工具轮次指标。** Ticket 03 最后一轮变更很窄，Ticket 04 的任务和 diff 不同，不能拿 12 次作为所有票据的效率标准。可以观察重复读取和无效轮次，不能因达到数字就少读证据。
- **把大批量读取当作越大越好。** 超大输出会再次截断，也可能引入大量不相关上下文；需要可控批次和完整性核验。
- **所有代理一律空历史。** 独立 reviewer 有明确隔离需求；implementer 或恢复原任务是否继承历史，应结合上下文相关性与重建成本决定。
- **因 reviewer 写了 Rejected 就新增完整 verdict 决策权。** `ticket-review` 当前要求 reviewer 返回覆盖和 findings，由 owning session 第 55–63 行形成正式 verdict。记录中的规范化 Block 符合这个职责划分。优先明确返回契约和 owner 校验，而不是让双方各形成一个权威 verdict。
- **删除复审、缩减必要测试或给修复轮次设硬上限。** 跟踪记录第 321–335 行的 Review 仍发现了三个实际需求缺口，没有证据支持这些削减。
- **仅凭累计输入判定成本或模型不适合。** 累计 token 不等于同时存在的上下文，也不能直接替代实际计费和端到端耗时。记录没有模型对照，不能据此固定某型号或断言更高推理档位可以解决调度问题。
- **把 429 归因于长上下文。** 记录只能支持中断后存在较大的非缓存输入，不能证明 429 的服务端根因或缓存失效机制。

### 需要进一步验证的可能重复检查

`ticket-execution` 第 23 行要求 merger 在组合实现上运行聚焦测试，而 `ticket-review` 第 63 行在 provisional Pass 后再次要求 owning session 运行测试。存在重复同一验证的设计可能，但这份记录没有证明发生了可避免的重复测试。

可后续评估：在实现、测试输入、环境条件和覆盖范围均一致时，允许验证门禁接收已有可核实执行证据；任何输入变化或证据不足仍需运行。此项涉及明确的现有执行要求，不应混在纯读取优化中悄悄改变。

### 代价、验证与结论变更

推荐先补容量、审查隔离与结果校验，再落实读取、等待和恢复约定，最后根据证据改善 Standards 覆盖。主代价是多一些调度状态、证据索引和恢复维护；对很小的票据，这些开销可能超过收益，应按任务规模采用紧凑形式。

后续用代表性任务比较完整通过率、有效缺陷与误报、端到端耗时、空等待次数、重复读取量、实际输入与输出用量、无 verdict 失败次数及修复轮次。重点覆盖：槽位不足、大 diff 截断、旧历史泄漏、覆盖缺项、中断恢复和正常小票据。当前没有执行这些行为实验。

此前推荐的“语义偏好加任务及上下文联合判断”不变。新运行证据说明仅增加模型选择策略不足以解决当前问题，新增的优先工作是调度、上下文和恢复规则。若后续确认历史运行没有加载现有规则，应优先修复规则传递和执行，而非继续堆叠技能正文；若代表性实验显示某项约定只增加开销而没有改善正确性或总耗时，就收窄或取消该项。

本轮仅追加本节讨论记录，原始运行报告、所有 SKILL 和关联正式文档保持不变。

## 原始日志核验

经用户授权，通过 SSH 从 `lima-debian-13` 只读拉取主会话 `01a06f81-edb6-7b00-9209-a05005071604` 及所属子代理，共 17 份 JSONL，99,069,580 字节。全部文件本地 SHA-256 与复制前远端清单一致；未复制同目录下其它会话或业务仓库。

本地目录：`/tmp/spec-implement-session-audit.fssILp`。目录含 `manifest.json`、只读统计脚本 `analyze.py`、原始日志，以及 `terra-findings.md`、`luna-usage-findings.md`、`terra-skill-findings.md` 三份分析报告。拉取后占用约 95 MiB，本机可用空间约 19 GiB。未为分析复制第二套原始日志。

按用户的成本要求，本次分工为两名 `gpt-5.6-terra / medium` 和一名 `gpt-5.6-luna`，主代理进行结构统计和结果汇总。以下用量全部属于被审计的历史业务执行，不是本次分析的 token 消耗。审计结论形成时尚未查询本次分析用量；随后已从本机主会话与三个子代理的 usage 记录核实，结果见开头汇总。

### 统一后的统计口径

统计窗口截止 `2026-09-05T07:18:56.999Z`。按各日志自身的 `thread_id` 筛选 `token_usage_record`，以 `response_id` 去重后累加 usage。`event_msg.token_count` 只作交叉核对，不再次累加；部分日志最后一个该事件比 usage 记录滞后一轮。

- 17 个业务会话的运行设置全部为 `gpt-5.6-sol / xhigh`。
- 合计 input 341,237,841，其中 cached input 330,908,032，约 97%；output 708,581。reasoning output 已包含于 output，不再叠加。
- 这些是跨响应累计输入，不是单次上下文，也不直接等于美元费用。

| 历史执行角色 | 累计 input | cached input | output |
| --- | ---: | ---: | ---: |
| root 调度器 | 103,831,282 | 102,023,040 | 61,118 |
| Ticket 03 merger | 61,286,405 | 60,649,600 | 110,112 |
| Ticket 03 implementer | 49,229,310 | 48,146,048 | 128,335 |
| Ticket 05 implementer | 31,598,285 | 29,519,360 | 89,915 |
| Ticket 01 implementer | 30,177,900 | 28,579,328 | 58,892 |

因此原报告称 Ticket 05 是“目前单个 Ticket 子代理中最显著的 token 消耗点”不成立。Ticket 03 的实现和 merger 更高，优化对象应覆盖整个调度和执行链。跨角色复杂度不同，这个排序只表示消耗分布，不能直接等同浪费比例。

根会话在 06:11 UTC 前调用 `wait_agent` 270 次，之后 29 次；之后 29 次中只有一次仍为 30 秒，其余为 180 秒。Ticket 05 的“128 次”是 122 次顶层 `exec` 加 6 次 `send_message`，内部实际为 70 次 `exec_command`、31 次 `apply_patch`、21 次 `write_stdin`，模型 usage 记录为 129 条。不能把这几个口径混用来制定工具轮次目标。

### 经原始证据确认或修正的事实

**已有规则确实进入过上下文。** 主会话日志第 11 行的 `spec-implement` 全文、第 19 行的 `ticket-execution` 输出，以及 Ticket 03 merger 日志第 43、45、46 行读取的技能文本，经核对与当前对应全文一致。上下文指针、容量预留和保持独立评审不是完全缺失的规则。需要区分规则未执行与规则确实没有描述的失败处理，不应统一归因于 skill 缺失。此结果不证明每个后续子代理都完整接收或遵守了相同规则。

**确认旧问题迟发现。** 第一轮 Ticket 03 reviewer 日志 `rollout-2026-09-05T04-47-19-01a06fe4-794a-7570-a7e6-c0b00a5e31fa.jsonl` 第 172 行（04:49:44 UTC）的固定版本源码已经包含共享错误码及名称字面量；第二轮日志 `rollout-2026-09-05T05-27-16-01a07009-0bed-7743-baa9-0a644061a879.jsonl` 第 168 行仍有相同内容。第三轮日志 `rollout-2026-09-05T05-53-01-01a07020-a114-7002-9804-6c533bcc6bbc.jsonl` 第 361 行（06:03:33 UTC）首次判为 Blocking。本次已排除“第二轮修复才引入该字面量”的解释，支持提高适用硬规范的覆盖核验优先级。

**确认 reviewer 创建失败及跨票复用尝试。** Ticket 04 merger 日志 `rollout-2026-09-05T06-43-51-01a0704f-28c5-7b90-a4f2-52a451c1f657.jsonl` 第 284、286 行记录线程上限拒绝；第 307、310 行记录向旧 Ticket 03 reviewer 发送 `followup_task`。07:04:15 UTC 的后续创建成功。失败瞬间各槽位究竟由谁占用，本次未完全确认，不能据此指定唯一容量根因。后续列表可以确认 root、Ticket 04 merger、reviewer、Ticket 05 implementer 四个运行者。

**历史配置不等于可测的完整历史继承。** 成功创建的 Ticket 04 reviewer 确实使用 `fork_turns: "all"`，metadata 有 `forked_from_id` 和 `subagent_history_start_ordinal=11`。但 ordinal 不是已经验证的 turn 数，不能称“继承 11 个 turn”。其首请求 input 仅 20,757，不能证明实际携带了 merger 的完整长历史。建议保持独立审查输入，仍应依据具体运行环境的历史共享语义；不把这次配置直接换算成继承 token 成本。

**预算翻倍没有解决截断。** Ticket 04 reviewer 日志 `rollout-2026-09-05T07-04-15-01a07061-d819-74e0-98b0-10061e1cd85b.jsonl` 第 23、26、30、33 行：两次读取相同固定范围的 diff，输出预算从 50,000 调到 100,000，但均返回 31,854 字符及相同截断警告，报告原始约 17,599 tokens、1,070 行。问题是重复取得同一段不完整证据，不是第二次预算更大就一定返回了更多内容。应在实际截断信号出现时改为未读范围或按文件分批。

**第四轮不是可直接比较的质量实验。** Ticket 03 最后一轮确实读取了规则、全范围 diff、实现和测试，并非只读修复片段；但 brief 明确聚焦 AC7，固定实现版本也不同。12 次调用不能证明与此前审查的覆盖质量等同或更高，仍不能作为统一目标。

### 核验后的调整顺序

1. **保留已有流程，强化调度交接的可核验条件。** 在实际创建 merger/reviewer 前确认当前阶段容量与必要输入；创建失败时区分暂时等待与无法释放容量，并明确现有 owning session 接管分支的安全移交。收益是减少停滞和错误复用；代价是少量阶段状态维护。
2. **提升适用硬规范覆盖的优先级。** 将原先 P2 的候选升为 P1：记录与当前 diff 相关的硬规则、证据和核对结果，与 AC 覆盖分别确认。变化依据是已证实旧问题至少跨两轮未被判出，影响验收可靠性标准。代价是增加紧凑的覆盖核验，不扩成整本规范的表格。
3. **为输入截断增加明确恢复方式。** 大输入先估量、可控分批；截断后读取缺失范围，避免通过无限调大预算重取相同内容。覆盖 implementer、merger 与 reviewer。收益有直接原始记录支撑；小任务保留直接读取路径，避免无谓预处理。
4. **保留等待、紧凑交接与恢复建议。** 根与 merger 的长上下文和空等待仍值得优化；具体等待时限遵守当前有效指令。不承诺仅缩短 `followup_task` 就能清除历史，也不把无 verdict 的失败当作修复轮次。

原先“只定义三档语义，由主代理结合任务及上下文自主选择”的设计方向不变。新增事实改变了效率调整的重点和 Standards 覆盖优先级，没有提供不同模型或推理级别的对照证据。全员已经使用 xhigh，也不证明 xhigh 导致这些问题；它说明提高档位不能被当作这些流程问题的已验证解决办法。

以上仍为建议，没有执行技能修改或前后对照实验。如果紧凑执行核验就能解决问题，优先采用该方案，而不是继续堆叠正文；如果某项额外清单只增加工具轮次却未改善完整性或总耗时，应收窄它。
